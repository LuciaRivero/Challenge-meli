const HOST = 'https://api.mercadolibre.com'
const BASE_URL_ITEMS  = `${HOST}/items`
const BASE_URL_CATEGORIES  = `${HOST}/categories`
const BASE_URL_SEARCH = `${HOST}/sites/MLA/search?`
const CATEGORY_FILTER_NAME = 'Categorías'

module.exports = {
    HOST,
    BASE_URL_ITEMS,
    BASE_URL_SEARCH,
    BASE_URL_CATEGORIES,
    CATEGORY_FILTER_NAME
}